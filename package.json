{
  "name": "repolens-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "dotenv": "^16.6.1",
    "hono": "^4.8.5"
  }
}
